const utility = {
    // helper1: function(){

    // },
    convertSectoDay: function(seconds){
      var days = Math.floor(seconds / 86400); //86400 seconds to 1 day
      return days;
    },
    // helper3: function(param1, param2){

    // }
}

export default utility;